#include <iostream>
#include <bits/stdc++.h>
using namespace std;


int main()
{

    // its just like maximum of non adjavent problem but with the catch that first and last house are also neighbors to each other
    // it will be solved jut like before just with two case if first house include then last house exc and vice versa
    return 0;
}